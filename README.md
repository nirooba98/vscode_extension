# Nirooba's Extension

A small VS Code extension that says hello when the extension host starts.

## Features

When VS Code finishes opening, the extension activates and shows a `Hello World`
information message. It also writes `Hello World` to the extension host console.

## Requirements

No additional setup is required.

## Release Notes

### 0.0.1

Initial release.
